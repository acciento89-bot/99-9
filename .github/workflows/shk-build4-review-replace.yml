name: SHK Build 4 Review Replace

on:
  push:
    branches: [main]
    paths:
      - '.github/shk-build4-review-request.json'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  replace-build-and-resubmit:
    runs-on: macos-26
    timeout-minutes: 30
    steps:
      - name: Checkout bridge
        uses: actions/checkout@v5
        with:
          path: bridge

      - name: Replace rejected SHK builds with Build 4 and resubmit
        env:
          ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
          ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
          ASC_PRIVATE_KEY_B64: ${{ secrets.ASC_PRIVATE_KEY_B64 }}
        shell: bash
        run: bash bridge/.github/scripts/shk_build4_review_replace.sh
